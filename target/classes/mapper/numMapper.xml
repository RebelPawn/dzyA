<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hp.controller.NumController">
<!--    &lt;!&ndash;返回集合&ndash;&gt;
    <resultMap id="numResult" type="com.hp.bean.Nummodel">
        <id property="id" column="id" />
        <result property="numId" column="numId" />
        <result property="data" column="SQ" />
    </resultMap>

    &lt;!&ndash;查询&ndash;&gt;
    <select id="selectSQ" resultMap="numResult" >
        select id,numId,SQ from numDate
    </select>-->

    <select id="selNum" resultType="int">
        select count(*) from numDate
    </select>



</mapper>